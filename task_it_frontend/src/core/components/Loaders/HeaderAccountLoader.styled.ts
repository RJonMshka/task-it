import { spinAnimation } from "./MainLoader.styled";
import styled from "styled-components";

export const StyledAHLoaderContainer = styled.div`
    width: 48px;
    height: 48px;
    border-radius: 50%;
    background: white;
    position: relative;
    right: 50px;
`;